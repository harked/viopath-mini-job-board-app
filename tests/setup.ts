import '@testing-library/jest-dom'

// Provide a noop console.error override for noise in tests (optional)
// console.error = (...args) => {
//   const msg = String(args[0] ?? '')
//   if (msg.includes('act(')) return
//   // eslint-disable-next-line no-console
//   console.warn(...args)
// }

